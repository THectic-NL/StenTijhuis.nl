# Copyright (C) 2026 Sten Tijhuis
# SPDX-License-Identifier: MIT
name: Code quality

on:
  push:
    branches: [main]
    paths:
      - 'webroot/**'
      - '.github/workflows/quality.yml'
  pull_request:
    branches: [main]

jobs:

  # HTMLHint: checks for structural errors, missing attributes, and accessibility
  html-lint:
    name: HTML linting
    runs-on: ubuntu-latest
    steps:
      - name: Check out source code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 'lts/*'

      - name: Install HTMLHint
        run: npm install -g htmlhint

      - name: Check HTML
        run: htmlhint "webroot/**/*.html"

  # actionlint: checks workflow files for syntax errors and incorrect expressions
  workflow-lint:
    name: Check workflow files
    runs-on: ubuntu-latest
    steps:
      - name: Check out source code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Download actionlint
        id: get_actionlint
        run: bash <(curl https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)
        shell: bash

      - name: Run actionlint
        run: ${{ steps.get_actionlint.outputs.executable }} -color
